# 更新记录

> 写给用户看：说清**这一版你能感觉到什么变化**，不列内部重构与打包细节。
> 版本号规则：`主.次.修`，rc 后缀为发布候选（试用版，可能还有问题）。

## 0.1.5（2026-09-02）

**改进**

- **启动时会检查有没有新版本**——打开应用几秒后在后台悄悄看一眼，有新版就在
  状态栏挂一条提示。不弹窗、不打断你、也不会自己下载；要不要装由你点那条提示决定。
  检查失败（离线、网络不通）不会打扰你。

> ⚠ 这一版**装上之后还不能自动升级**：升级需要新旧两版配合，
> 本版是"新旧两版"里的旧那一版。真正能一键升上去，要等下一版发布之后。

## 0.1.4（2026-09-02）

**改进**

- **走势图默认显示完整账面**——此前默认是精简模式，打开先看到的是删减过的
  走势线，要自己按 Ctrl+T 切一次才看得到全貌。现在反过来：默认给你全部，
  想看精简再切。
- **精简模式改用中枢判据**——切到精简时，**没有该级别中枢的走势，不再画该
  级别的走势线**。此前只按级别标色，容易让人以为"这里有一段该级别走势"，
  而实际上它并不成立。
- **片段库不再重复显示同一个片段**——服务端换用新的片段编号后，同一片段在
  你机器上可能同时存在新旧两个包，此前会在列表里出现两条。现在只显示一条
  （保留新的那份），旧包留在磁盘上但不再露面。

**修复**

- 更新片段时若装出来的版本号不对，会明确报错而不是默默算作成功。

## 0.1.3（2026-09-02）

**改进**

- **分解时会告诉你它做了什么**——此前有两种情况程序心里清楚、界面上却
  一声不吭，看起来就像"算得不对"：
  - 数据较长时只分解最近一段。现在会写明**已截取最近 N 天（原始数据 M 天）**，
    你能对上图里画的是哪一段。
  - 逐步回放没有出现时，现在会说明**本版采用全量分析，无逐步回放**，
    而不是让这个功能悄悄消失、不给任何交代。

**修复**

- 上面第二种情况此前和真正的故障混在一起处理，结果是**真出故障时也只在
  后台留一行记录**，界面上看不出来。现在两者分开：属于正常形态的说明给你看，
  真故障才当故障报。

## 0.1.2（2026-09-01）

**修复**

- **Windows 上不再出现两个鼠标指针**——部分显卡（实测 AMD 集显）在硬件加速下
  会画出第二个错位的指针，窗口还会周期性闪烁。已默认关闭该层加速，
  图表滚动不受影响。

- **通达信导入支持北交所**——此前 `bj` 开头的标的会被整个跳过（只认沪深）。
- **导入找不到数据时会说清原因**——此前不论路径打错、数据没下载、还是没勾
  1 分钟线，都报同一句"没找到数据"。现在分开说：路径读不到就提示检查路径，
  读到了但没数据就提示去通达信下载。

---

## 0.1.1（2026-09-01）

**修复**

- **登录状态现在会记住了**——此前每次启动都要重新登录。原因是启动时若后台
  暂时连不上，已保存的登录信息就不会被读取；现在无论后台通不通都会先读。
- **添加片段失败时会告诉你要登录**——此前只显示"添加失败"，无从知道该做什么。
  现在会弹出登录框说明原因，登录成功后自动继续你原来的操作。
- **取消登录后按钮不再卡住**——此前点了"加入"再取消登录，按钮会一直停在
  "添加中…"。
- **macOS 上能正常看到片段列表了**——此前 macOS 会拦截应用的网络请求且不报错，
  表现为"没有可选片段"。
- **设置页不再显示服务器地址**——那是内部信息，对使用没有帮助。

**改进**

- **同时只能打开一个窗口**——重复点击图标会切换到已打开的窗口，而不是再开一个。
  多个窗口同时运行可能导致笔记、练习记录互相覆盖。
- 损坏的片段包不再无声消失——会明确告诉你有几个读取失败，并提供移除按钮。

**已知限制**

- 自动更新功能的代码已就位，但**本版未启用**。应用仍会在有新版时于状态栏提示，
  需要手动下载安装。启用需等服务器支持 HTTPS。
- macOS 版未经 Apple 公证，首次打开需**右键点击图标 → 打开**。
- Windows 版未经代码签名，安装时会提示"Windows 已保护你的电脑"，
  需点"更多信息 → 仍要运行"。

---

## 0.1.0（2026-08-31）

首个版本。

- **学习**——精选走势片段配合渐进回放，看走势如何一拍一拍展开、转折在哪里确认
- **练习**——分级练习题，检验你对走势划分的判断
- **自研**——导入自己的数据（通达信 / CSV），用同一套引擎做分解，加标注和笔记
- 分解引擎随安装包附带，**下载安装后即可使用**，无需另行配置
- 笔记、练习记录、标注全部保存在你自己的电脑上，不上传

支持平台：Linux（AppImage / deb）、Windows 10+、macOS 11+（Intel 与 Apple Silicon）
